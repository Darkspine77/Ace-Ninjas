class statProfile { 
  float Health, maxHealth, Speed, Strength, Energy, maxEnergy, Armor;
  boolean alive = true;
  statProfile(float Health1, float Speed1, float Strenght1, float Energy1, float maxEnergy1, float Armor1) { 
    Health = Health1;
    maxHealth = Health1;
    Speed = Speed1;
    Strength = Strenght1;
    Energy = Energy1;
    maxEnergy = maxEnergy1;
    Armor = Armor1;
  }
}